# TimelineItem 时间轴项

时间轴的单条，需放在 NeoTimeline 内。用法见 [Timeline 时间轴](/components/timeline)。

## API

| 属性 | 说明 | 类型 |
|------|------|------|
| timestamp | 时间戳 | `string` |
| type | 节点类型 | `string` |

插槽：默认（内容）。
