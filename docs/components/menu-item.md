# MenuItem

Single menu item. Must be placed inside NeoMenu. See [Menu](/components/menu) for usage.

## API

| Prop | Description | Type |
|------|-------------|------|
| index | Unique identifier | `string` |
| disabled | Disabled | `boolean` |

Slots: `title`, default.
