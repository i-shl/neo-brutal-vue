# InputGroup 输入框组

将输入框与按钮拼接成单一的硬汉图腾模块。

## 基础用法

`<NeoInputGroup>` 包装器完美对其子元素对齐。它清除了子元素的内部圆角和单体阴影，并为所有项目拉起统一粗边框和实心阴影。这是搜索框和邮箱订阅的理想设计形态。

<demo vue="../../demos/input-group-basic.vue" />

## API

`NeoInputGroup` 通过底层 CSS 控制子元素的合并裁剪渲染引擎，不需要任何自定义 Props。
