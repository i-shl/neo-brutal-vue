# TabPane 标签页项

标签页的单个面板，需放在 NeoTabs 内。用法见 [Tabs 标签页](/components/tabs)。

## API

| 属性 | 说明 | 类型 |
|------|------|------|
| name | 唯一标识 | `string` |
| label | 标签文字 | `string` |
| disabled | 禁用 | `boolean` |
| closable | 是否可关闭 | `boolean` |

插槽：默认（面板内容）。
