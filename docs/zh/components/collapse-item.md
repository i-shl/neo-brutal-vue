# CollapseItem 折叠项

折叠面板的单项，需放在 NeoCollapse 内使用。用法见 [Collapse 折叠面板](/components/collapse)。

## API

| 属性 | 说明 | 类型 |
|------|------|------|
| name | 唯一标识 | `string` |
| title | 标题 | `string` |
| disabled | 禁用 | `boolean` |

插槽：默认（内容）、`title`（自定义标题）。
